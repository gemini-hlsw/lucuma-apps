# Memory

## User Preferences

- **Use Metals MCP for diagnostics**: Prefer `mcp__ide__getDiagnostics` over running `sbt compile` through the shell or grepping for errors. Metals is connected via VS Code and provides compilation errors/warnings from the sbt build server.
